# OPTIONAL: no key is needed to play or use the local scene director.
# Never put real keys in a ZIP, browser, GitHub repository or conversation.
GOODBOT_AI_ENABLED=0
OPENAI_API_KEY=
# Choose an available Responses API model that supports JSON Schema outputs.
OPENAI_MODEL=
# Hard cap per local server process (failed provider attempts count too).
GOODBOT_AI_MAX_CALLS=10
PORT=4174
