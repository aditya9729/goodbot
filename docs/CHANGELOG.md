# Apprentice 04 · version 0.4.0

- Added a blank, local, gradient-trained symbolic action ranker; no pretrained routine.
- Added three teaching lessons: cup collection, tea service and cupboard/towel delivery.
- Added explicit teaching consent, pre-decision records, execution matching, correction
  links, fresh-layout trials, visible intentions, Take over and data-only notebooks.
- Preserved strict ordinary campaign destinations; allowed compatible alternative
  destinations in authored apprentice lessons so model decisions matter.
- Added guardian/no-oracle, transfer, import-validation, ownership/undo, interruption,
  privacy and ordinary-UI training/rollout tests.
- Preserved camera hotfix 0.3.1, local visual recorder and independent pressure bench.
- No remote push, external model request, webcam test or physical-robot learning.

## Earlier releases (historical)

# Studio 03 camera hotfix — 0.3.1

Fix reverse-motion/view feedback; add explicit body-frame drive/turn/strafe controls;
rate-limit automatic heading changes; initialize facing indoors; preserve per-view
look/zoom; provide Center view/C; reuse static CPU shadows and GPU mesh uploads;
reduce CPU live-camera resolution while moving; restore idle detail; log new body-
frame requests in the existing consented episode stream. Add targeted controller,
renderer-cache and exact-bundle browser regressions. Also release the bench take synchronously on close/Escape and reset its consent.
No new task, camera capture
permission, tactile physics, network request, API key, repository push or deployment.

# Prototype 03 / Studio 03 — 0.3.0

Based on the delivered GOODBOT Prototype 02 source overlay. This does not assert
anything about newer commits in a remote repository.

Implemented: warmer shared room geometry/materials, slat wall/sconces/bench with
navigation footprint, correct base-relative visual robot/object rotation, head
camera alignment; dependency-free WebGL2 backend; explicit CPU renderer fallback;
fixed reciprocal-depth AO slope error and softer shadows; coherent RGB/depth/ID
capture; camera/source-raster calibration and state; normalized requests and
assisted completion stream; symbolic support/grasp transitions; local recording
consent/stop/discard/export; bounded ZIPs; independent analytic tactile bench;
archive decoder/PLY/preview tool; unit and browser recording regressions.

Preserved: six hotel scenarios, permissions/protected belongings, assisted controls,
local task director, optional AI adapter, music, optional webcam lifecycle and
one-item carrying. Original Driftfall is unchanged.

Not implemented: hotel physics contact solver, dynamic grasps/independent joints,
measured forces/torques, real tactile sensors, shear/slip, deformable cloth,
simultaneous wrist/head cameras, long-duration streaming, automatic research
uploads, actual policy training, multiplayer, new remote repository or deployment.
GPU runtime and physical-device validation are pending; see the current test report.
